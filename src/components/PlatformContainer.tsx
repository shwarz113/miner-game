import React from 'react';
import platform from "src/assets/images/platform.png";

type Props = {

};

export const PlatformContainer = (props: Props) => {
    return (
        <>
            <img src={platform} />
        </>
    );
};
